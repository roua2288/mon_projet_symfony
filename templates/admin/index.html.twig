{% extends 'base.html.twig' %}

{% block title %}Tableau de bord Admin{% endblock %}

{% block body %}

    <h2><i class="fas fa-home"></i> Tableau de bord</h2>
    <p>Bienvenue dans l'espace d'administration. Sélectionnez une section à gérer.</p>

    <div class="dashboard-cards">

        <div class="card">
            <h3><i class="fas fa-user-graduate"></i> Étudiants</h3>
            <p>Gérez les comptes étudiants, les inscriptions et les informations personnelles.</p>
            {#  <a href="{{ path('admin_etudiants') }}" class="btn btn-primary">
                <i class="fas fa-arrow-right"></i> Accéder
            </a>#}
        </div>

        <div class="card">
            <h3><i class="fas fa-chalkboard-teacher"></i> Enseignants</h3>
            <p>Gérez les comptes enseignants et leurs affectations aux différentes classes.</p>
            {#  <a href="{{ path('admin_enseignants') }}" class="btn btn-primary">
                <i class="fas fa-arrow-right"></i> Accéder
            </a>#}
        </div>

        <div class="card">
            <h3><i class="fas fa-calendar-alt"></i> Emplois du temps</h3>
            <p>Créez et modifiez les emplois du temps des classes et des enseignants.</p>
            <a href="{{ path('admin_emplois') }}" class="btn btn-primary">
                <i class="fas fa-arrow-right"></i> Accéder
            </a>
        </div>

        <div class="card">
            <h3><i class="fas fa-users"></i> Classes</h3>
            <p>Gérez les classes, les associations étudiants/enseignants et les groupes.</p>
            <a href="{{ path('admin_classes_index') }}" class="btn btn-primary">Gestion des classes</a>
        </div>

    </div>

{% endblock %}

<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de bord Admin - TEKUP</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="{{ asset('css/adminCss.css') }}">
</head>
<body>
<header>
    <div class="user-info">
        <i class="fas fa-user-shield"></i>
        <span>Administrateur</span>
    </div>
    <h1>Tableau de bord Admin</h1>
    <a href="#" class="btn btn-danger" onclick="alert('Fonctionnalité de déconnexion à configurer'); return false;">
        <i class="fas fa-sign-out-alt"></i> Déconnexion
    </a>
</header>

<nav class="admin-nav">
    <ul>
        <li><a href="{{ path('admin_dashboard') }}" class="active"><i class="fas fa-tachometer-alt"></i> Tableau de bord</a></li>
        <li><a href="{{ path('admin_etudiants') }}"><i class="fas fa-user-graduate"></i> Étudiants</a></li>
        <li><a href="{{ path('admin_enseignants') }}"><i class="fas fa-chalkboard-teacher"></i> Enseignants</a></li>
        <li><a href="{{ path('admin_emplois') }}"><i class="fas fa-calendar-alt"></i> Emplois du temps</a></li>
        <li><a href="{{ path('admin_classes') }}"><i class="fas fa-users"></i> Classes</a></li>
    </ul>
</nav>

<main>
    <h2><i class="fas fa-home"></i> Tableau de bord</h2>
    <p>Bienvenue dans l'espace d'administration. Sélectionnez une section à gérer.</p>

    <div class="dashboard-cards">
        <div class="card">
            <h3><i class="fas fa-user-graduate"></i> Étudiants</h3>
            <p>Gérez les comptes étudiants, les inscriptions et les informations personnelles.</p>
            <a href="{{ path('admin_etudiants') }}" class="btn btn-primary">
                <i class="fas fa-arrow-right"></i> Accéder
            </a>
        </div>

        <div class="card">
            <h3><i class="fas fa-chalkboard-teacher"></i> Enseignants</h3>
            <p>Gérez les comptes enseignants et leurs affectations aux différentes classes.</p>
            <a href="{{ path('admin_enseignants') }}" class="btn btn-primary">
                <i class="fas fa-arrow-right"></i> Accéder
            </a>
        </div>

        <div class="card">
            <h3><i class="fas fa-calendar-alt"></i> Emplois du temps</h3>
            <p>Créez et modifiez les emplois du temps des classes et des enseignants.</p>
            <a href="{{ path('admin_emplois') }}" class="btn btn-primary">
                <i class="fas fa-arrow-right"></i> Accéder
            </a>
        </div>

        <div class="card">
            <h3><i class="fas fa-users"></i> Classes</h3>
            <p>Gérez les classes, les associations étudiants/enseignants et les groupes.</p>
            <a href="{{ path('admin_classes') }}" class="btn btn-primary">
                <i class="fas fa-arrow-right"></i> Accéder
            </a>
        </div>

        <div class="card">
            <h3><i class="fas fa-bullhorn"></i> Annonces</h3>
            <p>Publiez des annonces pour les étudiants et les enseignants.</p>
            <a href="{{ path('admin_annonces') }}" class="btn btn-primary">
                <i class="fas fa-arrow-right"></i> Accéder
            </a>
        </div>

        <div class="card">
            <h3><i class="fas fa-cog"></i> Paramètres</h3>
            <p>Configurez les paramètres du système et les options d'administration.</p>
            <a href="{{ path('admin_parametres') }}" class="btn btn-primary">
                <i class="fas fa-arrow-right"></i> Accéder
            </a>
        </div>
    </div>
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

